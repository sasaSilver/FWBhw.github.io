/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    distDir: './dist',
    images: { unoptimized: true },
    basePath: "/FWBhw.github.io",
}
   
 export default nextConfig